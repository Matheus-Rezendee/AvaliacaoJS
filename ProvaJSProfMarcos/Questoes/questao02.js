function escreveMsg(){
    console.log("função delay")
}

function setTime(funcao){
    setTimeout(funcao, 5000) 

    /* O setTimeout é o responsavel por essa espera no print da mensagem
    separados por vírgula, passamos o parametro que queremos executar e
    em seguida o tempo de espera pra esta mensagem aparecer

     (medido em ms  1000ms = 1s)*/

}

